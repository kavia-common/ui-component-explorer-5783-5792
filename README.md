# ui-component-explorer-5783-5792

Note: Frontend is a Create React App project with Tailwind CSS in frontend_main.
Run the following in that directory before building:
- npm ci (CI) or npm install (local)
- npm run build